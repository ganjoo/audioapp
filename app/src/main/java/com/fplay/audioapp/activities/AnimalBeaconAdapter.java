package com.fplay.audioapp.activities;

import android.content.Context;
import android.widget.ListView;

import ufobeacon.adapter.UfoBeaconAdapter;

public class AnimalBeaconAdapter extends UfoBeaconAdapter {
    public AnimalBeaconAdapter(Context context, ListView deviceList) {
        super(context, deviceList);
    }
}
